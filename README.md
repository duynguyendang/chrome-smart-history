# chrome-smart-history
A Chrome extension (Manifest V3) that turns your browsing history into a **semantic knowledge base**. Pages you visit are embedded **on-device** with Google's EmbeddingGemma model and stored locally in IndexedDB. You can then search your history **by meaning**, not just keywords.
